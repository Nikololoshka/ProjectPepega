<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.github.nikololoshka.pepegaschedule">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--   Widgets   -->
        <receiver
            android:name=".widget.ScheduleAppWidget"
            android:label="@string/widget_schedule_name" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_schedule_app_info" />
        </receiver>

        <activity
            android:name=".widget.ScheduleAppWidgetConfigureActivity"
            android:label="@string/widget_configurator"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!--  Services   -->
        <service
            android:name=".schedule.repository.ScheduleDownloaderService"
            android:exported="false" />

        <service
            android:name=".widget.ScheduleAppWidgetRemoteService"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <!--  Activities   -->
        <activity
            android:name=".SplashActivity"
            android:theme="@style/AppTheme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".schedule.editor.PairEditorActivity"
            android:label="@string/pair_editor"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".schedule.editor.DateEditorActivity"
            android:label="@string/date_editor"
            android:parentActivityName=".schedule.editor.PairEditorActivity" />
        <activity
            android:name=".schedule.editor.ScheduleEditorActivity"
            android:label="@string/schedule_editor"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>