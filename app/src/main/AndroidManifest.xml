<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.github.nikololoshka.pepegaschedule">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".schedule.activities.PairEditorActivity"
            android:label="@string/pair_editor"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".schedule.activities.DateEditorActivity"
            android:label="@string/date_editor"
            android:parentActivityName=".schedule.activities.PairEditorActivity" />

        <activity
            android:name=".schedule.activities.ScheduleEditorActivity"
            android:label="@string/schedule_editor"
            android:parentActivityName=".MainActivity" />

    </application>

</manifest>